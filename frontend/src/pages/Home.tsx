import React from 'react';

import About from '../components/About';

const Home = (): JSX.Element => {
    return (
        <div id="home">
            <About />
        </div>
    );
};

export default Home;